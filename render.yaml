services:
  - type: web
    name: qwen-coder-proxy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn proxy_server:app --host 0.0.0.0 --port $PORT
    plan: free
